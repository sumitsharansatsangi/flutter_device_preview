name: device_preview_screenshot
version: 1.0.0
description: A plugin for device_preview that allows to take screenshots.
homepage: https://github.com/aloisdeniel/flutter_device_preview/plugins/device_preview_screenshot

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  device_preview: ^1.0.0
  flutter:
    sdk: flutter
  path: ^1.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.2
